version: '3.3'

services:

  postgres:
    ports:
      - '5432:5432'

  bot:
    build:
      context: bot
      dockerfile: Dockerfile_dev
    volumes:
      - ./bot:/app
      - ./.volumes/root:/root


  app:
    build:
      context: frontend
      dockerfile: Dockerfile_dev
    ports: ['80:80']
    volumes:
      - ./frontend:/app
      - ./.volumes/root:/root
