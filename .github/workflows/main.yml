name: CI

on:
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run CI
        run: |
          cp example_.env .env
          docker-compose run app bash devops/ci.bash
